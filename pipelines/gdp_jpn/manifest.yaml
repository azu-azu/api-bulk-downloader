defaults:
  output_root: outputs/  # relative to CWD where wdi-pipeline is invoked
  export_format: csv
jobs:
- job_id: gdp_jpn
  enabled: false  # set to true to include this job in run-all
  connector_params:
    indicator_code: NY.GDP.MKTP.CD
    country_code: JPN
  sql:
    file: queries/timeseries.sql
    params:
      min_year: '2000'
  export:
    filename: gdp_jpn
    format: csv
  schema:
    file: schemas/timeseries.yaml
